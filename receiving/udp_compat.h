#ifndef OUVR_UDP_COMPAT_H
#define OUVR_UDP_COMPAT_H
/* UDP network that is compatible with receiving from unmodified ffmpeg */
#include "ouvr_packet.h"

struct ouvr_network udp_compat_handler;

#endif